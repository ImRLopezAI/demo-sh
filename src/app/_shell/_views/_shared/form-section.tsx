/**
 * Re-export shim — canonical location is @/components/ui/json-render/form-section.
 * This file exists only so non-migrated _views modules keep compiling.
 * Delete once all consumers import from the canonical path.
 */
export { FormSection } from '@/components/ui/json-render/form-section'
